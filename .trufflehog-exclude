# Paths the secret scanner skips. Every line carries a reason; nothing is added
# here to quieten a finding.

# Not source. Commit history is scanned by gitleaks, which reads commits.
^\.git/
^target/

# Test fixture asserting that embedded credentials are discarded.
^crates/topgent-core/tests/suite/inventory\.rs$
